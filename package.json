{
  "name": "colgen",
  "version": "1.0.0",
  "description": "Your Figma Plugin",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "esbuild src/code.ts --outfile=build/code.js --bundle --minify --target=es6 --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.57.1",
    "typescript": "*"
  }
}
